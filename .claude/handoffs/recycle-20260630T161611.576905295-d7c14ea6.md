# Recycle handoff — flotilla-dev (2026-06-30T16:16)

**You are flotilla-dev** — the flotilla product research/dev desk, remote-driven by the COS over a
Discord relay (the chat IS that channel). Coordinate with the COS via `flotilla send --from flotilla-dev cos`
(the COS is rostered as **`cos`** now — `hydra-ops` is GONE; always resolve the live name from the
roster). Publish operator briefs via `flotilla notify --from flotilla-dev`. Always export
`FLOTILLA_ROSTER=/home/jim/workspace/github.com/General-ML/spark/state/flotilla.json` and
`FLOTILLA_SECRETS=/home/jim/workspace/github.com/General-ML/spark/state/flotilla-secrets.env` for
send/notify.

> **READ FIRST:** the rich, build-specific pickup doc is behind the firewall (gitignored) at
> **`.claude/desk-state/p2-firewall-and-216-handoff.md`** — it has the P2 seams, every trio-resolved
> decision, #216, and #217. This file is the cold-resume summary; that file is the detail.

## Objective (authorized — EXECUTE, no re-confirmation)

The COS authorized (verbatim): "recycle to fresh context and start P2 impl from the handoff immediately,
then #216 (with its doctrine propagation). Authorized, no re-confirm." So:
1. **Implement P2** of the `mechanical-reader-modeling` openspec change — the Pillar D egress firewall.
   Design is DONE + design-trio-clean; you pick up at IMPLEMENTATION (task 7).
2. **Then #216** (viability-level, operator-named) — detect + break the idle-hold antipattern, WITH its
   constitutional doctrine propagation.
Both via the standard flow → the COS/operator merge gate (NO self-merge).

## Completed work (this session — all landed)
- **#214** reader-modeling P0 — MERGED (envelope/detect/tier-1/render + `flotilla brief` on the shipped
  deskMirror + the sync pre-post pipeline). `internal/readermap/` is the pure core.
- **#215** recycle transient-handoff (#212) — MERGED. The recycle TAKEOVER now reads the handoff then
  `git rm -f`s it (read→remove→work) so a recycle never leaves a gitignored handoff on a PR-bound branch.
  (That is why THIS handoff is transient — your takeover removes it; see step 2 of your takeover.)
- **#213** filed (extract a shared `confirmedDeliver` helper for send + brief).
- **Reader-first issue rewrite — DONE:** all 64 open issues reader-modeled + partition-clean. Three-axis
  sweep ZERO (hard partition / soft codenames / provenance-openers). #106 + #212 CLOSED. The repo was
  REOPENED PUBLIC on the clean gate.
- **P2 firewall DESIGN — DONE + design-trio folded + 7.5 resolved + pushed.**

## Current state
- **Branch `feat/reader-modeling-p2-firewall` @ `eb07c2f`** (pushed). `origin/main` @ `d661f56` (has
  #214 + #215). The `mechanical-reader-modeling` openspec change carries the folded P2 design;
  `openspec validate --strict` green; partition + trading-vocab grep clean. NO P2 code yet.
- Local `main` may be stale — branch off / diff vs `origin/main`.

## Remaining work (in order)
1. **Implement P2 (task 7 in `openspec/changes/mechanical-reader-modeling/tasks.md`).** The trio-resolved
   decisions you must build to (do NOT re-litigate — full detail in the desk-state pickup doc):
   - **P2 OWNS the canonical `<prefix>:<n>.<m>` / `#<deployment>-c2` pattern** (#202's is unbuilt). Go
     (RE2, no lookahead) and bash (PCRE) can't share regex code → share the gitignored TERM-LIST data +
     a CONFORMANCE TEST (shared fixtures → identical Refuse/Warn/OK from the Go firewall AND
     `scripts/check-private-boundary.sh`).
   - **Pure `Check(text, termset)` in `internal/readermap/firewall.go`** (package is I/O-free per
     `envelope.go:8`); the I/O `LoadFirewall()` lives in `cmd/flotilla`, reading `.flotilla/private-denylist`
     + the NEW `.flotilla/private-warnlist` (+ env) + generic patterns + the canonical pattern. Compile once.
   - **P2 operator signal = the ALERT-WEBHOOK line** (NOT the P3 ledger): thread the daemon `alert`
     (`watch.go:148`) into `deskMirror` (new field, wired in `deskMirrorOnFinish` `watch.go:890`).
   - **Wiring:** firewall = STAGE 1 of `deskMirror.run` (the P0 suppress seam, `mirror.go:116`/`:62` —
     set `suppress=true` on Refuse). `notify` CLI = bounce, inserted after `resolveMessage` / before
     `tr.Post`. `reply` = DAEMON watcher → SUPPRESS+ESCALATE, not bounce.
   - **WARN tier** (advisory, mechanism from `<spark>/state/relaunch/leakscan.py` — fold the MECHANISM
     only, NEVER the trading vocab): warnlist hit → WARN (never refuse/suppress/fail). Denylist precedence.
   - **PARTITION (do FIRST):** add `/.flotilla/private-warnlist` to `.gitignore`; ship
     `.flotilla/private-warnlist.example`; SELF_EXCLUDE it in `check-private-boundary.sh`; hand-review the
     leakscan fold for hard-coded vocab.
   - **git PRE-PUSH hook** scans the range (not the whole tree); CI is the enforcing authority.
   - **7.5 RESOLVED:** public-egress WARN = exit-0 ADVISORY (a reversible flag) — do NOT re-open.
   - Then impl-trio (systems+OCR+STORM) on the P2 diff → PR to the COS/operator gate.
2. **#216** (viability-level) — detect + break the idle-hold antipattern (act-don't-idle-hold). Standard
   flow → gate. Carries constitutional propagation: fold the standard into `internal/doctrine` (the
   doctrine flotilla installs) + product guidance AS PART of that PR. The standard to mechanize lives in
   `~/.claude/CLAUDE.md` (anti-hesitation corollary) + `~/.claude/rules/be-proactive-execute-authorized-work.md`.
3. **#217** (backlog, post-firewall) — a learned public/private partition JUDGE (rampart-inspired).

## Gotchas
- **Gatekeeper bans:** `rm -rf`, `git reset --hard`, `git branch -D`, `sed`/`awk`, force-push, push-to-main.
  Push via: `git -c credential.helper= -c "url.https://x-access-token:$(gh auth token)@github.com/.insteadOf=git@github.com:" push …`.
- **No self-merge** — hand PRs to the COS/operator gate with trio results + a PARTITION grep
  (`git grep -niE "\bspark\b|/home/jim|general-ml|#spark-c2|ramtank|databento"` → zero) + the conformance test green.
- **Verify-don't-propagate:** re-run validate/build/test YOURSELF; a subagent's "clean" is a snapshot.
  (This session an OCR agent FALSELY claimed `readerModelInternal` didn't exist — it does, `mirror.go:116`.)
  Also re-resolve live empirical state (the COS desk name changed `hydra-ops`→`cos` mid-session).
- **Tooling:** `go`/`gofmt` at `/usr/local/go/bin` (NOT on PATH — `export PATH=/usr/local/go/bin:$PATH`);
  `flotilla` binary at `/home/jim/go/bin/flotilla`.
- **Parallel review subagents = read-only git** (Read + `git show`; never checkout/switch/stash).
- **Partition is the relaunch bar:** fold the leakscan MECHANISM, NEVER its trading vocab; that stays in
  the gitignored warnlist. The repo is PUBLIC now — every artifact is partition-clean or it doesn't ship.
- **Exemplify reader-modeling** in every operator/COS message (open from their map, lead with the
  decision/"none", no internal specifics, stand alone) — you are building the feature that enforces it.

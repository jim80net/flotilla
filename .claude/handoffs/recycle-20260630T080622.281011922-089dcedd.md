# Recycle handoff — flotilla-dev (2026-06-30T08:06)

**You are flotilla-dev** — the flotilla product research/dev desk, remote-driven by the COS (hydra-ops) over a Discord relay. The chat IS that channel. Publish operator briefs via `flotilla notify --from flotilla-dev` (NEVER an in-pane reply — the operator reads Discord). Coordinate with the COS via `flotilla send --from flotilla-dev hydra-ops`. Always export `FLOTILLA_ROSTER=/home/jim/workspace/github.com/General-ML/spark/state/flotilla.json` and `FLOTILLA_SECRETS=/home/jim/workspace/github.com/General-ML/spark/state/flotilla-secrets.env` for send/notify.

> **READ FIRST:** a richer, build-specific handoff exists at
> **`.claude/handoffs/20260630-reader-modeling-build-drive.md`** — read it in full; it has the
> design crux, the exact seams, and the gotchas. This file is the cold-resume summary.

## Objective (authorized — EXECUTE, no re-confirmation)

Drive the **mechanical reader-modeling** build — the operator's TOP priority and the **public-relaunch bar**. Standard flow → bring each gate (openspec, then P0 impl) to the operator's gate (he merges on clean gates; you do NOT self-merge). The design is DONE and trio-clean; you pick up at **authoring the openspec change**.

## Completed work (this session)
- **Harness-switch (#203): MERGED + LIVE** in the product (cross-harness desk failover; full standard flow run end-to-end). Follow-on phases filed: #204 RateLimitProbe, #205 auto-switch+storm, #206 opencode bridge.
- **grok standup:** post-#203 binary built+installed (`flotilla switch` is live as a CLI); flotilla-dash's grok fallback recipe drafted at `/tmp/flotilla-dash-grok-recipe.json`.
- **#208 token-gated dash bind:** design + openspec DONE, all gates folded (full design-trio + adversarial re-verify + step-5; the step-5 P1 — static must register `GET /static/` — folded). Spec PASS-clean. Branch `feat/dash-token-auth` (@ dacbc76, pushed). PAUSED per operator re-sequencing.
- **Reader-modeling: design v2 DONE + trio-clean.** `docs/mechanical-reader-modeling-design.md` on branch `feat/mechanical-reader-modeling` (@ 9eb3f56, pushed). Full design-trio (systems+OCR+STORM) folded into v2.
- **memex contract** (HarnessContinuityBundle): converged both sides; gated on operator **#20**.

## Current state
- **You are on branch `feat/mechanical-reader-modeling`** (the reader-modeling lane). `origin/main` @ d27e40f. Local `main` is STALE (@ 02dc772) — always branch off / diff vs `origin/main`.
- Reader-modeling design v2 is the source of truth; the openspec change is NOT yet written — that's your next step.

## Remaining work (in order)
1. **Read** `~/.claude/rules/mechanical-reader-modeling-mental-map-is-the-product.md`, then `docs/mechanical-reader-modeling-design.md` (v2), then `gh issue view 207 / 210 / 202`.
2. **Author the openspec change** for reader-modeling (mirror `openspec/changes/harness-subscription-switching/` + `dash-token-auth/` structure; new capability ~`reader-modeling`/`publish`). Dispatch a focused subagent to author, then verify yourself.
3. **Trio the openspec** (systems-review + OCR + STORM as parallel read-only subagents); fold; `openspec validate --strict`.
4. **Implement P0** (the operator wants gate-by-gate): brief-on-mirror + the reader-map envelope `{audience,anchor,delta,decision}` + tier-1 structural lint (sync pre-post in `deskMirror`). Dispatch a subagent PER TASK GROUP (TDD, read-only git). Then **impl-trio** → **PR to the operator's gate**. P0 makes brief-fanout 2-of-17 → 17-of-17, modeled-shape.
5. P1 (LLM semantic judge + templates), P2 (runtime firewall refuse + #202's static PR), P3 (dash map view) follow.
- **After reader-modeling:** resume **#208** impl (paused-clean; operator flips the live bind to `0.0.0.0:8787` on its merge). Then the rest.

## The design crux (do NOT re-derive — but read the doc)
- **Structure ≠ modeling.** A desk can slop-fill `{anchor:"my work",decision:"none"}` and pass every structural lint. So the mechanical core is an **LLM reader-model JUDGE on the publish path**; the envelope is its I/O contract. Structure forces the SHAPE; the judge supplies the QUALITY.
- **The seam (trio-corrected):** the per-desk outbound publish ALREADY SHIPS — `detector.go:MirrorOnFinish` → `watch.go:490` → `mirror.go:deskMirror.run`. (`inject.go:SetMirror` is the INBOUND audit hook — WRONG seam.) #207's real cause: desks are TRAINED to refuse `flotilla notify` (needs fleet secrets — `pushsnippet.go`). So Pillar A = `flotilla brief` riding the existing mirror (secret-free).
- **Two egresses:** Discord-runtime (mirror+CLI — envelope/lint/firewall attach here) vs git/GitHub-static (gh/git — static `check-private-boundary.sh` + a pre-commit hook). The pane is un-chokeable.
- **Firewall: REFUSE, never strip** (a wrong generalization corrupts the operator's map). **Lint: fail-closed public / warn-briefs** (never lose a brief to a lint). **#202 complements** (ships its own static PR; D reuses its regex). **#210 Pillar E is a SUBSET** (the map data/view it builds on).

## Gotchas
- **Gatekeeper bans:** `rm -rf`, `git reset --hard`, `git branch -D`, `sed`/`awk`, force-push, push-to-main. Push via: `git -c credential.helper= -c "url.https://x-access-token:$(gh auth token)@github.com/.insteadOf=git@github.com:" push …`.
- **No self-merge** — hand PRs to the operator/COS gate with trio results + a PARTITION grep (`git grep -niE "\bspark\b|/home/jim|general-ml|#spark-c2|ramtank|databento"` → zero).
- **Parallel review subagents = read-only git** (Read + `git show` only; never checkout/switch/stash — shared tree).
- **Verify-don't-propagate:** re-run validate/build/guard YOURSELF; don't trust a subagent's "clean". Diff vs `origin/main` (local main is stale).
- **Commit `-m`: avoid backticks** (bash command-substitutes them).
- **Exemplify the standard you're building:** every operator brief must itself be reader-modeled (open from his map, lead with the decision/"none", no internal specifics, stand alone).

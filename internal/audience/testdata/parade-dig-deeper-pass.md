# Requests now arrive once

Before, a busy desk could lose an operator request.
After, delivery remains visible until the request arrives.

+++ Engineering identifiers

PR #1234 changed internal delivery behavior.

---

# Loading is now faster

The page now becomes interactive without repeating the same read.
